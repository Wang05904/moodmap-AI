## 🚀 快速开始

### 环境配置
1. 复制环境变量模板：
```bash
cd backcode
cp .env.example .env
```
## 启动前端文件指令：
  ```bash
  cd frontcode
  npm install
  npm run dev
  ```
## 启动后端接口指令：
  ```bash
  cd backcode
  npm install
  node app.js
  ```